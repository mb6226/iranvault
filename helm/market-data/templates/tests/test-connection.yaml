apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "market-data.fullname" . }}-test-connection"
  labels:
    {{- include "market-data.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "market-data.fullname" . }}:{{ .Values.service.port }}/health']